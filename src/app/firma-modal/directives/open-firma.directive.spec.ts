import { OpenFirmaDirective } from './open-firma.directive';

describe('OpenFirmaDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenFirmaDirective();
    expect(directive).toBeTruthy();
  });
});
